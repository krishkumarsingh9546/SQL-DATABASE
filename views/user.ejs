<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All users</title>
    <style>
        table {
            width: 70%;
            border-collapse: collapse;
            margin: 20px auto;
        }

        table, th, td {
            border: 1px solid black;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }
        .user{
            text-align: center;
            color: beige;
            padding: 10px;
            background-color: rgb(232, 78, 78);
            border-radius: 15px;
        }
    </style>

</head>
<body>
       <h2 style="text-align: center;">Add a new user</h2>
        <form style="text-align: center; " method="get" action="/user/add">
            <button class="user">Add newuser</button>
        </form>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Email</th>
                <th>Password</th>
            </tr>
        </thead>
        <tbody>
            <% for(user of result){ %>
                <tr>
                    <td><%= user.id %></td>
                    <td><%= user.username %></td>
                    <td><%= user.email %></td>
                    <td><%= user.password %></td>
                    <td style="padding: 12px;">
                    <form method="get" action="/user/<%= user.id %>/edit" >
                       <button style="border-radius: 10px;  background-color: rgb(8, 180, 57);"> Edit username </button>
                    </form>
                   </td>
                   <td style="padding: 12px;"> 
                    <form  method="post" action="/user/<%= user.id %>?_method=DELETE">
                        <button style="border-radius: 10px; padding: 10px; background-color: rgb(186, 47, 47);">DELETE</button>
                    </form>
                   </td>
                </tr>
           <% } %> 
        </tbody>
    </table>

</body>
</html>